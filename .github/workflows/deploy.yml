name: deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: "publish"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: "Checkout latest state on server"
        shell: bash
        run: |
          ssh ${{ secrets.SSH_HOST }} "cd project/api-monitor ; git fetch --all ; git checkout main ; git pull --rebase"